[package]
name = "grid_lab"
version = "0.1.0"
edition.workspace = true
publish = false

[dependencies]
eframe = { workspace = true, features = ["glow"] }
finui-grid = { path = "../../crates/finui-grid" }
